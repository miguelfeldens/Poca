# Backend API URL — in development, the Vite proxy rewrites /api to localhost:8000
# In production, set this to your Cloud Run URL
VITE_API_URL=http://localhost:8000

# WebSocket URL for the Gemini Live chat endpoint
VITE_WS_URL=ws://localhost:8000
